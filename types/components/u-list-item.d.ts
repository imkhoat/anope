export interface UListItem {
    as?: string,
    to?: string | object,
    dense?: boolean,
    icon?: string,
    title?: string,
    description?: string,
    activeClass?: string,
    isActive?: boolean,
    badge?: string,
    value?: string,
}