export interface ListItem {
    as?: string,
    to?: string | object,
    dense?: boolean,
    icon?: string,
    title?: string,
    description?: string,
    activeClass?: string,
    hoverClass?: string,
    isActive?: boolean,
    badge?: string,
    value?: string,
    collapse?: boolean
}